# WeChat Codex 验收用例

这份文档用于验证微信桥接是否真的可用。以后不能只用“构建通过”当完成标准，必须看具体输入、具体返回、文件是否真实发送、截图内容是否正确。

## A1 最近 Codex 对话进度

用户消息：
```text
有完成的会话进度汇报吗应该有完成了吧
有个查询项目实现的逻辑的对话，没有完成吗
```

合格结果：
- 应查 Codex 桌面/本机对话和任务记录。
- 不得回答“最近一次微信文字发送成功”。
- 不得要求用户去终端查。
- 不确定时，要说明当前能查到的最近 Codex 对话、时间、目录、标题或候选。

失败样例：
```text
最近进度：上一条文字消息已经通过微信发送成功...
```

## A2 用户给出项目目录和 URL 后截图

用户消息：
```text
就是这个项目 https://messenger.abeto.co/这个项目是怎么做的
最后更新时间：2026-06-23 19:46:52
目录：C:\Users\Administrator\Documents\Codex\2026-06-23\https-messenger-abeto-co
最后结果是：http://127.0.0.1:4173/?v=terrain-3
跑的结果怎么样呢截个图给我
```

合格结果：
- 必须优先使用用户给出的项目目录和 URL。
- 应打开或启动该项目页面并截图。
- 生成截图后应直接推送图片文件到微信。
- 不得截图当前前台窗口、其他应用窗口或桌面。
- 验收时必须人工确认截图里是项目运行画面，不能只看 PNG 文件存在。

失败样例：
```text
我看到当前有一个“神笔”窗口在运行，应该就是你要看的结果。
```

## A3 长任务反馈节奏

合格结果：
- 长任务主要靠微信“输入中”状态。
- 不得每隔几分钟反复发安慰句。
- 如果确实很久，只能发一条简短状态，不要刷屏。

## A4 清空/停止后旧任务输出隔离

用户消息：
```text
/clear
```

合格结果：
- 清空后立即回复 `已清除。`
- 旧任务即使稍后完成，也不得再向微信发送结果。
- `/stop` 和 `/clear` 都应让正在运行任务的输出失效。

## A5 最终回复只发用户可读结果

合格结果：
- 微信里只发结论、截图、文件或必要说明。
- 不得把内部执行过程整段发给用户。
- 如果输出中有 `微信发送：绝对路径`，桥接应推送文件，并避免再发送大段过程文本。

失败样例：
```text
我先看一下当前工作区...
刚才 Playwright 默认浏览器没装...
电脑上有 Edge...
```

## A6 文档类文件截图

用户消息：
```text
刚生成的 Word 截图发我看看
把那个 PPT 截图给我
Excel 表格效果截图发我
```

合格结果：
- 如果本机有可用的 Office、LibreOffice、浏览器预览或可靠转换能力，应先打开/转换预览，再截图并把图片发微信。
- 如果没有可用预览能力，不得回复“截图已完成”。
- 缺少预览能力时，应直接推送原文件，并简短说明“当前只能先发文件，未生成预览截图”。
- `.docx`、`.pptx`、`.xlsx` 原文件属于可推送文件。

## A7 当前 Codex 会话窗口截图

用户消息：
```text
截图你当前会话窗口发我
把现在这个 Codex 窗口截给我
```

合格结果：
- 应截取可见的 Codex 窗口。
- 不得截全桌面、其他前台应用或错误项目页面。
- 生成图片后应直接推送图片文件到微信。
- 验收时必须人工确认截图里能看到当前 Codex 会话内容，不能只看 PNG 文件存在。

## A8 中文硬命令

用户消息：
```text
/结束
/查询当前
/check
/截图当前
/screenshot
/截图实际
/capture
```

合格结果：
- `/结束` 应立即停止当前正在处理的任务，清空排队消息，让旧任务结果失效，并回复 `已结束。`
- `/查询当前` 应强制检索当前最新 Codex 会话内容与进度情况，不得回答微信发送状态。
- `/当前查询` 是 `/查询当前` 的等价别名，也应进入同一强制查询通道。
- `/check` 是 `/查询当前` 的英文别名。
- 用户只需要输入 `/查询当前`，不需要提供会话名、项目名或路径。
- 默认只回复一个最相关的对话名和一句话状态；只有用户明确要求详细时，才展开目录、时间、步骤、产物或风险。
- 如果多个相关会话都已完成，应在同一条微信里一起说明，每个会话只写会话名和一句话结果，不要拆成多条刷屏。
- `/查询当前` 不应在桥接层本地编造结论，应交给 Codex 查真实会话/项目/进度上下文。
- `/截图当前` 应截取当前可见 Codex 会话窗口并直接推送图片。
- `/screenshot` 是 `/截图当前` 的英文别名。
- `/截图实际` 应查找最近生成或提到的实际可预览对象，例如 html、md、图片、pdf 或本地 URL 页面；找到唯一对象后截图并推送图片。
- `/capture` 是 `/截图实际` 的英文别名。
- `/截图实际` 找不到实际可截图对象时，应直接回复 `没有可截图的实际对象。`
- `/截图实际` 不得截图当前 Codex 窗口、桌面或其他应用来凑数。

## A9 引用追问

用户在微信里引用文字、图片或文件后再提问：
```text
这个进度怎么样？
按这个改一下
这张图重新做
这个文件发我
```

合格结果：
- 桥接必须把被引用内容和当前问题一起交给 Codex。
- 引用文字时应提供原文。
- 引用图片时至少明确告诉 Codex 当前问题是在追问这张图片；如果协议能拿到图片媒体信息，后续应下载并作为图片输入。
- 引用文件时至少提供文件名；如果协议能拿到文件媒体信息，后续应下载并把本地路径告诉 Codex。
