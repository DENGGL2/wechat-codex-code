# WeChat Codex 功能修改前必读

这份文档记录用户已经反复确认过的产品规则。后续改微信桥接功能前，必须先读这里，不能凭当前一条问题临时补规则。

## 核心原则

微信桥接不是另一个低配机器人。桥接层只负责收发、排队、状态、文件图片传递、前台输入和必要的本地控制。

用户语义必须交给 Codex 判断，包括：

- 普通聊天和咨询。
- 项目、对话、进度追问。
- 引用消息后的追问。
- “继续改一下”“按这个调整”“再优化”。
- 沟通偏好，例如“以后简短点”“不要每步都发微信”“说话别机械”。
- 模糊文件请求，例如“把桌面那个 XXX 文件发我”。

不要再在桥接层堆本地语义规则。之前反复变蠢的根因，就是看到一个坏回复就往桥接里补一个判断，最后规则互相误伤。

## 命令和自然语言

微信端日常使用必须以自然语言为主。

可以保留隐藏兜底控制：

- `/stop`：紧急停止当前任务。
- `/clear`：清空当前微信会话。

不要要求用户记命令来完成正常任务。尤其不要要求用户输入精确文件路径。用户更可能说：

- “把桌面那个 XXX 文件发我”
- “把刚生成的 PPT 发我”
- “截图发我看看”
- “继续改刚才那个”

这些都应该交给 Codex 理解和执行。

## 完成通知

锁屏后，桌面 Codex 对话完成时，可以推送微信通知。格式应该自然、短：

```text
XXXX对话已完成
一句话说明完成内容
```

重点规则：

- 要告诉用户“哪个对话”的“什么内容”完成了。
- 最优先读取真实对话标题。
- 其次读取首条用户消息、预览、rollout 里的用户请求或上下文，组合出用户能看懂的标题。
- 不能把 UUID、threadId、会话 ID 当标题发给用户。
- 不能发乱码、技能名、旧通道文案、`????`、`已完成一轮处理` 这类废话。
- 取不到真实标题不等于直接不推；应该继续找可读来源，尽量生成用户能理解的标题。
- 只有在标题和结论都完全无法读懂时，才可以跳过推送。
- 微信里用户主动发起的任务，已经回复最终答案后，不要再额外补一条“当前对话已完成”。

完成通知不是为了凑数推送，而是为了让用户锁屏后回来能知道：哪个对话完成了，结果大概是什么。

## 追问上下文

桌面完成通知的上下文和微信当前聊天上下文要分开保存。

用户收到推送后可能会问：

- “进展怎么样了”
- “刚才那个继续改”
- “按这个调整”
- “发我文件”

这些要能接到上一条完成通知对应的桌面 Codex 对话。

但用户也可能问另一个问题。不能把所有追问都硬绑到上一条完成通知，要让 Codex 根据语义判断是不是同一个任务。

## 引用消息

用户在微信里引用一条文字、图片、文件或视频再追问时，桥接必须把“被引用内容”和“当前新问题”一起交给 Codex。

已有基础规则：

- 引用文字时，prompt 要明确区分“用户引用了以下消息”和“用户当前问题”。
- 引用图片时，不能只告诉 Codex“引用了一张图片”就结束；如果协议里能拿到图片媒体信息，应下载图片并作为图片输入传给 Codex。
- 引用文件时，不能只告诉 Codex 文件名；如果协议里能拿到文件媒体信息，应下载文件并把本地路径告诉 Codex。
- 引用视频暂时可以先标记为视频引用，但不能假装已经看过视频内容。
- 回复时尽量引用用户所问的上下文来回答，不要答成孤立新消息。

引用追问仍然属于语义理解，不要在桥接层写“如果引用了什么就固定回复什么”的本地规则。桥接只负责把引用内容完整、结构化地转给 Codex。

## 微信回复节奏

不要每一步都推微信。

- 能短答就短答。
- 长任务先显示输入中即可，不要刷很多“收到、开始、处理中”。
- 如果确实超过较久，可以发一条简短状态，不要连续发多条。
- 长回复合并后再发，别把用户微信刷屏。
- 生成图片、文件、截图完成后，能直接发内容就发内容，不要只发本地地址，除非用户明确要地址。

## 图片、语音、文件

- 用户只发图片时，先收着，不要立刻回复“收到图 1 图 2”。
- 等用户再用文字或语音说明要做什么，再开始处理。
- 语音转文字后应和前后消息合并理解，不要只看最后几个字。
- 文件上传后要能保存、读取，并按用户意图处理。
- 需要发回文件时，Codex 可以输出明确的发送标记，桥接负责发文件。

## “帮我输入”

“帮我输入 XXX”是特殊功能：把 XXX 填入当前前台 Codex 输入框，不自动发送。

触发应尽量只在句首，避免用户句中提到“帮我输入”时误伤。

成功回复保持很短：

```text
已填入
```

## 改功能前检查

每次改微信桥接功能前，先确认有没有违反这些边界：

- 是不是又把语义判断写进桥接层了？
- 是不是又要求用户记命令、给精确路径、按机器方式说话？
- 完成通知是否能读出真实对话名和一句话结论？
- 有没有可能发 UUID、乱码、技能名、旧通道文案？
- 微信主动任务是否会被重复推送“已完成”？
- 桌面完成通知上下文是否会被普通微信聊天覆盖？

改完后至少跑：

```bash
npm run build
```

并搜索旧问题关键词，确认没有明显回退。
